# Rust
/target/

# Tile cache of the aerial imagery overlay
/cache/

# Shots from --screenshot
/screenshots/

# Configuration written on first start (may hold your own access keys)
/imagery.ron
# The model registry of the imagery detection, and the weights it points at.
# Neither may travel with the repository: the file names local paths, and a
# detector is tens of megabytes under a licence of its own (README, *Detecting
# from the aerial imagery*).
/ai.ron
/models/

# Operating system
Thumbs.db
.DS_Store

# Python tools (tools/*.py)
__pycache__/

# The forest measurement writes a throw-away mod (tools/trees/bench_forest.mjs).
mods/_forest_bench/
