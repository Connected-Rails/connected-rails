# Rust
/target/

# Tile cache of the aerial imagery overlay
/cache/

# Shots from --screenshot
/screenshots/

# Configuration written on first start (may hold your own access keys)
/imagery.ron
# The model registry of the imagery detection: written on first start, and it
# names local paths, so it belongs to the machine rather than to the repository.
# The weights it points at *do* travel — `models/` is tracked, through Git LFS,
# and `models/LICENSES.md` records what each one costs (README, *Detecting from
# the aerial imagery*).
/ai.ron

# Operating system
Thumbs.db
.DS_Store

# Python tools (tools/*.py)
__pycache__/

# The forest measurement writes a throw-away mod (tools/trees/bench_forest.mjs).
mods/_forest_bench/
